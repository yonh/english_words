<h1>Listing words</h1>
 
<%= link_to 'New Word', new_word_path%>

<table>
  <tr>
    <th>Word</th>
    <th></th>
  </tr>
 
  <% @words.each do |word| %>
    <tr>
      <td><%= word.word%></td>
      
	  <td><%= link_to 'Show', word_path(word) %></td>
	  <td><%= link_to 'Edit', edit_word_path(word) %></td>
	  <td><%= link_to 'Delete', word_path(word), 
	  				method: :delete, data: { confirm: 'Are you Sure?' } %></td>

    </tr>
  <% end %>
</table>
